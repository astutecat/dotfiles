{
  "default": {
    "prefix": "default",
    "body": [
      "[private]",
      "default:",
      "  @just --justfile '{{justfile()}}' --list",
      ""
    ],
    "description": "default justfile"
  }
}
